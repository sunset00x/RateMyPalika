import{useEffect,useState}from"react";import{useParams,Link}from"react-router-dom";import{api}from"../services/api";export default function Detail(){const{id}=useParams();const[d,setD]=useState<any>();useEffect(()=>{api.get("/municipalities/"+id).then(r=>setD(r.data.data))},[id]);if(!d)return <main className="page">Loading...</main>;return <main className="page"><Link to="/municipalities">← Back</Link><h1>{d.name}</h1><p>{d.district.name}, {d.district.province.name}</p><div className="stats"><div><b>{d.scores?.[0]?.overallScore??"—"}</b><span>Overall Score</span></div><div><b>{d.wards.length}</b><span>Wards</span></div><div><b>{d.projects.length}</b><span>Projects</span></div><div><b>{d.budgets.length}</b><span>Budgets</span></div></div><section className="card"><h2>Performance</h2><p>Verified indicator data will populate this section.</p></section></main>}