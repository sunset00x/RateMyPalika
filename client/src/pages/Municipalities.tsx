import{useEffect,useState}from"react";import{Link}from"react-router-dom";import{api}from"../services/api";export default function Municipalities(){const[a,setA]=useState<any[]>([]);useEffect(()=>{api.get("/municipalities").then(r=>setA(r.data.data))},[]);return <main className="page"><span>EXPLORE</span><h1>Municipalities</h1><div className="grid">{a.map(x=><Link className="card" to={"/municipalities/"+x.id} key={x.id}><small>{x.type}</small><h2>{x.name}</h2><p>{x.district.name}, {x.district.province.name}</p></Link>)}</div></main>}