import{useEffect,useState}from"react";import{api}from"../services/api";export default function Compare(){const[a,setA]=useState<any[]>([]),[s,setS]=useState<number[]>([]);useEffect(()=>{api.get("/municipalities").then(r=>setA(r.data.data))},[]);return <main className="page"><span>COMPARE</span><h1>Compare municipalities</h1><div className="grid">{a.map(x=><button className={"card "+(s.includes(x.id)?"selected":"")} onClick={()=>setS(v=>v.includes(x.id)?v.filter(i=>i!==x.id):v.length<5?[...v,x.id]:v)} key={x.id}>{x.name}<small>{x.district.name}</small></button>)}</div></main>}